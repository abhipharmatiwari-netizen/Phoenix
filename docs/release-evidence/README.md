# Release evidence bundles are written here by scripts/capture_release_evidence.ps1
# Each bundle is named <timestamp>-evidence.json.
# These files are excluded from the release artifact but should be attached to PRs/deployment records.

