[pytest]
markers =
    smoke: Fast pre-deploy smoke tests — import checks, config lint, startup validation.
           Run with: pytest -m smoke
           These replace the deleted backtest_smoke.sh and serve as the lightweight
           pre-deploy gate documented in docs/runbooks/release_evidence.md.
